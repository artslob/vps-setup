[Unit]
Description=MTProxy
After=network.target

[Service]
Type=simple
WorkingDirectory={{ proxy_dir }}
ExecStart={{ proxy_dir }}/run-mtproxy.sh
Restart=on-failure

[Install]
WantedBy=multi-user.target
